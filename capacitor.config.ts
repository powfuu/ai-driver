import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ev.ai.driver',
  appName: 'AutoEscuela: AI Driver',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
};

export default config;
